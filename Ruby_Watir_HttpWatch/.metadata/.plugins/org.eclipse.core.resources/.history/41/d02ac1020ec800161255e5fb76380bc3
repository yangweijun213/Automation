require 'logger'
require File.dirname(__FILE__) + '/Common'
 
class LoggerFactory
  
  def getLogger(name)
      logFileName = "#{name}_Common.new().getStrftime()"
      #Create a logger
      logger = Logger.new("#{name}_Common.new().getStrftime()")
      
      #输出不同级别的日志
      logger.level = Logger::INFO
      
      return logger
    end
    
end